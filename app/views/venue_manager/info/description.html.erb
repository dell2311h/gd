<%= render :layout => 'venue_manager/shared_container', :locals => {:selected => :info} do %>

<div class="form">
    <%= render :partial => 'venue_manager/shared/info_menu' %>
    <section class="selected-tegs">
        <div class="tags">
            <ul>
                <li>description</li>
            </ul>
        </div>
        <div class="location">

        </div>
    </section>
    <section class="manager-content-table">
        <%= form_for :venue, :url => description_venue_manager_info_path do |f|%>
        <fieldset class="description">
            <label>
                <span>2000 characters maximum</span>
                <%= f.text_area :description, :placeholder => "venue edescription/boilerplate", :class => "description", :onkeyup => "updateTFCount(this, 2000)"%>
                <div class="annotation">characters remaining: <span style="display: inline;" id="venue_description_count">2000</a></div>
            </label>
        </fieldset>
        
        <div class="buttons">
            <button class="orange"><%= t(:button_save_changes) %></button>
            <button class="gray">cancel</button>
        </div>
        <% end %>
    </section>
    <div class="decoration"></div>
</div>
<% end %>
<script>
  $(document).ready(function(){
    updateTFCount($('#venue_description'), 2000);
  });
</script>